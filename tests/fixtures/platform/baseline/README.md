# SecRL Lite Baseline Fixtures

Task 1 records provenance only. No production trajectory or generated
no-trunc result has been copied into this directory.

The canonical source baseline is the manually approved commit
`9c36eae065978ac2b52ee772814b345b66702253`. A future fixture added here must
identify its source revision and include a SHA-256 of the original artifact;
it must not contain API keys, local configuration, generated
`failure_analysis_outputs_no_trunc`, or `final_results_no_trunc` content.

See `docs/baselines/secrl-lite-baseline.md` for the decision evidence and
`docs/baselines/secrl-lite-files.sha256` for the canonical source manifest.
